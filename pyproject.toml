[project]
name = "pt5-handler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.14.10",
    "syrupy>=5.0.0",
]

[tool.pytest]
minversion = "9.0"
pythonpath = ["test", "src"]
testpaths = ["test"]
